#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int a, d; 
    cin >> a >> d; 

    if (a > d) {
        cout << "No" << endl; 
    } else {
        cout << "Yes" << endl; 
    }

    return 0;
}