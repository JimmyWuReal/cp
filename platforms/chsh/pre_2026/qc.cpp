// Code written by Jimmy Wu (JingZe Wu) 
// AC during the contest 

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n; 
    cin >> n; 

    while (n--) {
        int a, b; 
        cin >> a >> b; 
    }

    cout << "YES" << endl; 

    return 0;
}